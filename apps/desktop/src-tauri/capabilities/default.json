{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core and dialog access for the main window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
