[package]
name = "phx-annot"
version = "0.0.1"
edition.workspace = true
license.workspace = true
description = "Tiers (interval/point), typed hierarchy (parent/child spans on the ELAN model), validation of cross-tier integrity"

[dependencies]
