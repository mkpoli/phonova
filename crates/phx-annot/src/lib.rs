//! Tiers (interval/point), typed hierarchy (parent/child spans on the ELAN
//! model), validation of cross-tier integrity.
#![warn(missing_docs)]

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        assert!(true);
    }
}
