//! Gaussian-window STFT power spectral density; viewport-independent tile
//! computation in dB.
#![warn(missing_docs)]

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        assert!(true);
    }
}
