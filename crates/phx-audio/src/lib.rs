//! `Audio` (planar f32 samples + f64 sample rate), WAV via hound, broad
//! decode via symphonia (opt-in features; MPL-2.0 unmodified), resampling
//! via rubato.
#![warn(missing_docs)]

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        assert!(true);
    }
}
