[package]
name = "phx-render"
version = "0.0.1"
edition.workspace = true
license.workspace = true
description = "Perceptual colormaps (viridis, magma, grayscale), theme-aware dB to RGBA tile rendering"

[dependencies]
