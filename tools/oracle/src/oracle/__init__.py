"""Out-of-process Praat oracle for phonia's Rust analysis crates.

Wraps praat-parselmouth (GPLv3) as a standalone dev/CI tool. Nothing here
is imported by, linked into, or shipped inside any Rust crate — see
`docs/plan/validation.md` and `docs/research/algorithms-and-validation.md`
§7.1 for the licensing reasoning.
"""
